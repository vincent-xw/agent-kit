node_modules/
dist/
coverage/
.wrangler/
.dev.vars
# 真实密钥只留在本地；.env.example 是模板，可以提交。
.env
.env.local
!.env.example
*.sqlite
*.sqlite-shm
*.sqlite-wal
# BFF 按日轮转文件日志目录（运行时在 dist/log，由 dist/ 覆盖；此处兜底源码/打包场景）
examples/flutter-dev-bff/log/
.pnpm-store/
.pkg-cache/
# ime-setup.sh 记录的切换前输入法，仅本地有意义
.ime-previous
.DS_Store
